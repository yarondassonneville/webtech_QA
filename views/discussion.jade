html
  head
    title= title
  body
    h1= topic
    h2= date
    form(method="post", action="")
      label(for="newDiscussion") New Discussion
      input(type="text", name="newDiscussion", value="dummy")
    ul
      for discussion in allDiscussions  
        h1 #{discussion.topic}
